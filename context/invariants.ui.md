# Frontend invariants

checked: <date>

Rule categories that must hold on the frontend, and where each is enforced or defined.
The citation is authoritative; the rule text is a hint, verify before relying. Lines
marked "gap" are rules stated somewhere but not mechanically enforced.

## Data flow

- <the chain, e.g. pages -> hooks -> services -> API> -> `<path>`

## API contract consumption

- <rule> -> `<path>`

## Types

- <rule> -> `<path>`

## Routing

- <rule> -> `<path>`

<!-- SKELETON: structure only. Categories are examples - keep what applies, add what's
     missing. Content fill is a separate session. -->
