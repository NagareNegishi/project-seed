# Backend invariants

checked: <date>

Rule categories that must hold on the backend, and where each is enforced or defined.
The citation is authoritative; the rule text is a hint, verify before relying. Lines
marked "gap" are rules the code states but does not fully enforce.

## Auth & authorization

- <rule> -> `<path>`

## Data model gotchas

- <rule> -> `<path>`

## Validation

- <rule> -> `<path>`

## Fail-fast config

- <rule> -> `<path>`

## Error responses

- <rule> -> `<path>`

<!-- SKELETON: structure only. Categories are examples - keep what applies, add what's
     missing. Content fill is a separate session. -->
