# Local environment — never commit real values; the pattern lives in .env.example
.devcontainer/.env

# Claude session notes — local observation list; promoted to CLAUDE.md or a
# skill only after user review, never committed
docs/notes.md
