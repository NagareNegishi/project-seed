---
paths:
  - "<backend-dir>/**"
---

<!-- Path-scoped rule file: rules below apply only when Claude works on files
     matching `paths`. Copy this file per area (backend/frontend/tests) and
     replace the placeholders. -->

- <rule that applies only inside <backend-dir> — one line each>
