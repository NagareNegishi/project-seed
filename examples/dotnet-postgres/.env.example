# Copy to .devcontainer/.env (gitignored). docker-compose reads it automatically.
# Consumed by the db service in docker-compose.yml.
POSTGRES_USER=<user>
POSTGRES_PASSWORD=<password>
POSTGRES_DB=<db>
