# Copy to `.env` in this folder (gitignored). docker-compose reads it automatically.
# The generic seed needs no variables — stacks add theirs when copied in from examples/.
#
# Example (from examples/dotnet-postgres):
# POSTGRES_USER=<user>
# POSTGRES_PASSWORD=<password>
# POSTGRES_DB=<db>
